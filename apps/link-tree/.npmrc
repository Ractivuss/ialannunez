# This file exists for documentation but won't be used by Cloudflare Pages
# Cloudflare Pages runs npm install from workspace root
# Use build command: npm install --legacy-peer-deps && npx nx build:production link-tree
legacy-peer-deps=true